# Observation-only bootstrap include. It does not redirect DNS or traffic.
# A committed data-plane revision atomically replaces this file.
log-queries=extra
log-async=25
log-facility=/var/run/dnsmasq/router-policy-observations.log
