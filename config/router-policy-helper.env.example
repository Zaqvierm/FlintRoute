# Reference only. Production installs create the product-owned helper.env
# with the pinned daemon UID; do not change the peer UID without changing the
# controller service identity and protocol tests together.
peer_uid=1001
socket=/var/run/router-policy/helper.sock
