# Optional root-helper boundary. Do not enable until the controller is running
# as this exact non-root UID. The installer never creates this file silently.
peer_uid=1001
socket=/var/run/router-policy/helper.sock
